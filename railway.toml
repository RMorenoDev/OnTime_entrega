[build]
builder = "nixpacks"
watchPatterns = ["**/*.php", "**/*.js", "**/*.jsx"]

[deploy]
startCommand = "bash backend/deploy.sh && php backend/artisan serve --host=0.0.0.0 --port=$PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
